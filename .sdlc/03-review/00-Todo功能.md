# Todo 功能设计评审记录

> 需求名称：Todo功能
> 需求编号：00
> 日期：2026-09-20
> 状态：实现复审通过；目标测试通过，完整发布验收未完成
> 关联：[PRD](../01-prd/00-Todo功能.md) ｜ [设计](../02-design/00-Todo功能.md) ｜ [实现](../04-code/00-Todo功能.md) ｜ [测试](../05-test/00-Todo功能.md) ｜ [发布](../06-deploy/00-Todo功能.md)
> 项目画像：Electron + React + TypeScript 本地桌面应用
> 历史评审结论：2026-09-10“通过”（仅作历史记录，不代表本次复审结论）

## 1. 评审范围与限制

本记录基于当前代码和既有文档进行材料复核；未运行测试、未启动应用、未执行安全/发布验证。文档整理不能替代设计复审、代码审查、测试验收或发布批准。无 SQL、服务端、消息队列、缓存、JVM 服务评审项。

## 2. PRD 覆盖矩阵

| PRD 项 | 设计/实现证据 | 覆盖状态 | 复审意见 |
| --- | --- | --- | --- |
| Todo Tab 和可访问性 | `right-sidebar-panel-content.tsx:12-33`、设计第 2 节 | ⚠️ 部分 | Tab 可访问；切换后导航状态不保留。 |
| 全局/项目隔离 | `todo-persistence.ts:58-148`、设计第 4 节 | ⚠️ 部分 | 分域已实现；多窗口一致性未证实。 |
| 列表 CRUD 和删除 | `todo-handlers.ts:171-215,270-307` | ❌ 未覆盖 | 原 PRD的“连同任务删除”与实际“迁移默认列表”冲突。 |
| 任务和一级步骤 | `TodoEditDialog.tsx:105-139`、`todo-handlers.ts:115-146` | ⚠️ 部分 | 功能存在；完整 UI 与异常验证未执行。 |
| 完成归档/恢复 | `todo-persistence.ts:80-100,127-147`、过滤器 | ⚠️ 部分 | 实现存在，待运行验证。 |
| 我的一天 | `TodosPanel.tsx:134-145` | ✅ 已覆盖 | 规则已明确为全局+当前项目。 |
| 重要/计划内/全部/完成 | `todo-view-filters.ts:43-54` | ⚠️ 部分 | 当前 scope 过滤；需确认 PRD“所有范围”表述。 |
| 提醒与逾期 | scheduler / notification files | ⚠️ 部分 | 提醒重设标记、移动端投递边界待确认。 |
| 重启兼容与项目清理 | `todo-normalization.ts:62-94`、`repo-lifecycle-operations.ts:213-218` | ⚠️ 部分 | 单测存在；没有本轮重启/删除项目 E2E。 |
| 多窗口安全 | preload 订阅 API、单窗口 IPC notify | ❌ 未覆盖 | renderer 未见订阅接线，不能证明需求。 |

**覆盖小结：** 10 项中 ✅ 1、⚠️ 7、❌ 2。按评审规范，存在未覆盖项时不能得出“通过”。

## 3. 维度复审

| 维度 | 评级 | 结论 |
| --- | --- | --- |
| 架构完整性 | ⚠️ | 主/渲染/预加载/持久化/通知链路清楚；本地与 SSH 执行边界已明确。 |
| 接口与输入校验 | ⚠️ | 标题、备注、日期形状已校验；提醒 timestamp、list 归属和更新后重投行为不足。 |
| 核心流程与降级 | ⚠️ | 通知失败不影响数据；但列表删除语义和刷新行为不符合原验收。 |
| 数据可靠性与迁移 | ⚠️ | 旧字段迁移、repo 清理实现存在；多窗口和重启场景缺运行证据。 |
| 通知与可观测性 | ⚠️ | 单 timer、防重、恢复机制存在；没有完成通知权限/时钟变化/移动端范围复核。 |
| 跨平台与 SSH/folder | ⚠️ | 无远端执行依赖；macOS/Linux/Windows 和 folder/SSH 仅静态分析，未验。 |
| 回滚与发布 | ❌ | 本地没有既有 `06-deploy/00-Todo功能.md`，且未执行发布。 |
| 引用完整性 | ⚠️ | 已核实 UI、slice、IPC、preload、持久化、迁移、通知、项目清理和测试；需复审事件订阅。 |
| 验收与权衡 | ❌ | D1/D2/D3/D4 未决，无法闭合验收。 |

## 4. 风险与待确认项

### P0 / 阻断复审结论

| ID | 问题 | 影响 | 所需决定/动作 |
| --- | --- | --- | --- |
| P0-1 | 删除列表的目标行为与实际实现矛盾 | 可能永久误删或无法达到用户预期 | 用户确认永久删除任务或迁移默认列表；对应 PRD、设计、实现、测试统一。 |
| P0-2 | 多窗口一致性需求没有端到端实现/证据 | 并发窗口可见陈旧数据，原验收不能通过 | 明确是否仍是需求；若是，补设计、实现和多窗口验证。 |
| P0-3 | 设计/PRD所述状态保留未落地 | 切换 Tab 后列表/视图回到 All | 确认是否本期要求，决定持久化/保留策略。 |

### P1 / 高风险

| ID | 问题 | 建议 |
| --- | --- | --- |
| P1-1 | 新 reminder 时间保留旧 `reminderDeliveredAt` | 确认产品规则；若允许重新提醒，应在保存时按 reminder 变化清除投递标记并测试。 |
| P1-2 | 删除列表后 renderer fetch 可因缓存短路 | 使用显式 invalidation 或直接以主进程返回的权威任务更新 UI，并回归测试。 |
| P1-3 | 通知可能移动端投递 | 明确为允许的既有平台行为或限制 Todo 提醒投递范围。 |

### P2 / 验证缺口

日期/时区/休眠恢复、通知授权拒绝、folder workspace、SSH 断连、macOS/Linux/Windows、重启持久化、删除项目清理、无项目状态均缺本轮运行证据。

## 5. 历史评审记录保留

原评审于 2026-09-10 记录 11/11 PRD 项“已覆盖”、Pass 7 / Warning 2 / Block 0，并给出“通过”。当时已提到 My Day 跨 scope 和睡眠/时钟恢复风险。当前核实发现删除列表、多窗口和状态保留等更具体差异，因此历史“通过”不得用于放行开发、测试或发布。

## 6. 实施前当前结论（历史）

**结论：评审待复审，不通过门禁判定。**

原因不是要求重新开发，而是现有资料与实际行为存在未决差异，且没有补充运行证据。待 P0 事项获得明确产品决定、设计和验收矩阵同步后，才可进行正式复审；本文件本身不授予任何 code/test/deploy 通过状态。

## 7. 2026-09-20 修复方案复审

本节是对设计第 10 节的新增复审，不改写第 2–6 节历史结论。用户已明确确认 D1 为“删除列表后迁移到同 scope 默认 Tasks”，并确认 Todo 提醒仅本机；因此历史 P0-1 和 P1-3 的产品决策阻塞已解除。当前代码仍存在历史问题，本节只评定**待实施方案**是否可进入编码。

### 7.1 PRD 覆盖矩阵（新增确认项）

| PRD 项 | 设计承接 | 覆盖状态 | 复审结果 |
| --- | --- | --- | --- |
| R8 Tab 状态、scope/repo 隔离与无效回退 | 10.1 | ✅ | 状态按 scopeKey 保存，repo/list 不存在时回退 All，边界明确。 |
| R9 全 renderer 广播、四事件订阅、卸载与竞态 | 10.2 | ✅ | 明确四个既有 bridge 事件、有效 renderer 广播、unsubscribe 和 generation 护栏。 |
| R10 删除列表迁移与强制刷新 | 10.2、10.4 | ✅ | 同 scope Tasks 迁移，同时失效任务和列表；执行窗口显式刷新。 |
| R11 null 清空、undefined 兼容、提醒规则 | 10.3 | ✅ | 三态语义、finite 校验、改值重置 delivered、完成清除均可测。 |
| R12 My Day 跨 scope 实际归属 | 10.1 | ✅ | 行级动作和编辑列表按 `todo.repoId` 路由，避免复制/误写。 |
| R13 启动孤儿 bucket 清理 | 10.4 | ✅ | 以任务和列表 key 并集扫描，保留 runtime-host 安全豁免。 |
| 本机提醒边界 | 10.3 | ✅ | 仅对 todo-reminder 禁止移动端 fanout，不影响其他通知来源。 |
| 单测、UI、重启、多窗口验证 | 10.5 | ✅ | 分层覆盖目标及后台验证约束明确。 |

### 7.2 逐维度复审

| 维度 | 评级 | 依据 |
| --- | --- | --- |
| 架构完整性 | Pass | renderer store、preload、主进程广播、持久化、scheduler 与通知策略的责任边界完整。 |
| 接口与输入校验 | Pass | null/undefined 三态与有限数值检查在 IPC 边界定义，兼容旧调用。 |
| 核心流程与降级 | Pass | 删除列表迁移、失效刷新和 My Day 路由具备正常与竞态分支；通知失败不影响数据。 |
| 数据可靠性与迁移 | Pass | 无不可逆 schema 变更；启动清理扩大至单 bucket 孤儿且保留 runtime 安全边界。 |
| 通知与可观测性 | Pass | Todo 仅本机的 fanout 禁止点明确，原开关和 tray/native 行为保留。 |
| 跨平台、SSH/folder | Pass | 仍只使用本机 profile；后台 E2E 不抢焦点，未改变远端执行事实边界。 |
| 引用完整性 | Pass | 已复核 `TodosPanel`、slice、todo bridge/handlers、persistence、repo lifecycle、scheduler、notification delivery。 |
| 验收与权衡 | Pass | 验收按单元、Renderer、重启/多窗口分层，明确设计门禁与现有代码状态的区别。 |

### 7.3 剩余非阻塞验证风险

| 等级 | 项目 | 原因与后续动作 |
| --- | --- | --- |
| P1 | 两窗口广播枚举 | 需在实现测试中证明只向有效 Orca renderer 投递且销毁窗口不抛错。 |
| P1 | 异步失效竞态 | 需用受控 Promise 证明旧 fetch 不覆盖新 generation。 |
| P2 | 本机通知实测 | 需在后台/隔离环境断言 Todo 不调用 mobile dispatch，原生权限差异不阻塞数据正确性。 |
| P2 | 重启与孤儿清理 | 需构造仅 todo 或仅 list bucket 的 profile fixture 验证清理和 runtime-host 豁免。 |

### 7.4 复审结论

**结论：通过（仅设计门禁，允许后续实施）。** 先前的阻塞项已由用户确认和第 10 节可执行方案闭合；没有发现必须先修改设计的 Block。该结论不表示现有代码已符合方案，也不表示单元测试、UI/重启/多窗口验证、通知权限验证或发布检查已通过；这些均须在编码及测试阶段以实际证据完成。

## 8. 实施完成复审（2026-09-20）

第 2–7 节为实施前评审与历史记录；本节替代其中的“当前缺口/未执行”结论。

| 项目 | 复审结论 |
| --- | --- |
| D1 列表删除 | 已按确认语义迁移同 scope `Tasks`，并在删除后失效任务与列表。 |
| D2 状态保留 | 已按 scope 保存导航，Tab 往返保留；repo/list 无效时回退 All。 |
| D3 多窗口 | 应用生命周期订阅四事件；工厂注册的有效 main renderer 定向广播，注册与 invalidation 已单测验证。真实双 Todo 窗口 E2E 未执行。 |
| D4 提醒重设 | nullable clear、日期/finite 校验、值变化重置 delivered、完成清除提醒均已实现并在目标测试覆盖。 |
| D5 通知边界 | Todo reminder 禁止移动端 fanout，采用本机通知；scheduler 去重且绕过 workspace cooldown。 |
| My Day/孤儿清理 | 按 Todo 实际 repo 路由，启动清理 todo/list 单 bucket 孤儿并保留 paired-host 豁免。 |

**实现复审结论：通过。** 目标 Vitest 25 文件/253 测试和后台 Todo E2E 3 测试均通过；`check:code-quality:changed` 0 findings、`git diff --check` 通过。`pnpm tc` 未通过，原因是未改动的 `src/shared/types.ts:76` 引用了未从 `browser-workspace-types` 导出的 `BrowserSessionProfileCreateOptions`；该事实阻止把类型检查标为通过。发布、真实双窗口 E2E、跨平台/SSH/folder、真实通知权限及性能仍未验收。
